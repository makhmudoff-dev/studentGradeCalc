const calculate = () => {
  let maths = document.getElementById("math");
  let comp = document.getElementById("comp");
  let phy = document.getElementById("phy");

  let grades = "";

  let totalgrades = parseFloat(maths) + parseFloat(comp) + parseFloat(phy);
};
